<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.com.helei.bot.core.mvc.mapper.AccountBaseInfoMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="cn.com.helei.bot.core.entity.AccountBaseInfo">
        <id column="id" property="id"/>
        <result column="type" property="type"/>
        <result column="name" property="name"/>
        <result column="email" property="email"/>
        <result column="password" property="password"/>
        <result column="params" property="params"/>
        <result column="insert_datetime" property="insertDatetime"/>
        <result column="update_datetime" property="updateDatetime"/>
        <result column="is_valid" property="isValid"/>
    </resultMap>

    <select id="insertOrUpdate" parameterType="cn.com.helei.bot.core.entity.AccountBaseInfo" resultType="java.lang.Integer">
        INSERT INTO t_account_base_info
        (id, type, name, email, password, params, insert_datetime, update_datetime, is_valid)
        VALUES (NULL, #{type}, #{name}, #{email}, #{password}, #{params}, #{insertDatetime}, #{updateDatetime}, #{isValid}) ON CONFLICT(email)
        DO
        UPDATE SET
            type = excluded.type,
            name = excluded.name,
            password = excluded.password,
            params = excluded.params,
            update_datetime = excluded.update_datetime
    </select>
</mapper>
